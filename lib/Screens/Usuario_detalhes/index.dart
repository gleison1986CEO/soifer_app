import 'package:flutter/material.dart';

class Usuario_ extends StatelessWidget {
  const Usuario_({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('header sistema soifer'),
      ),
      body: Center(
        child: Text("teste"),
      ),
    );
  }
}
